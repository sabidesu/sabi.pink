# sabi.pink (11ty version)

this is the repo for the 11ty/new version of my webbed site

## getting started

make sure you have nvm installed

1. after cloning this repo and entering the directory, run `nvm use` to use the appropriate version of node
2. run `npm install` to install all necessary packages
3. you should be able to start the web server with `npm start`
